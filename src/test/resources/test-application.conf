products {

  database {
    url = "jdbc:postgresql:test_products"
    user = "postgres"
    password = "just-password"
    driver = "org.postgresql.Driver"
  }

  http {
    host = "localhost"
    port = 9999
  }

}
